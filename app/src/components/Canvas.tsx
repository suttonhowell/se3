import { Activity } from '../pages/Editor/components/Activity';

export const Canvas = () => {
  return (
    <svg
      style={{
        height: '100%',
        width: '100%'
      }}
    >
      <Activity />
    </svg>
  );
};
